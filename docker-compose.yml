version: '3.8'

services:
  pickleball-app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
    volumes:
      - ./data:/app/server/data
    restart: unless-stopped
